apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "skoed.fullname" . }}
  labels:
    {{- include "skoed.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | nindent 4 }}
