# In place upgrade

This chapter is a placeholder. Contributions welcome — open a PR
adding it under `docs/src/$(echo "docs/src/operations/in-place-upgrade.md" | sed 's|docs/src/||')` and
the docs workflow will publish.

In the meantime, the canonical reference is the spec under
`specs/technical/` in the repo.
